
# This file is automatically included by OMNeT++'s Makefile.
#  (https://doc.omnetpp.org/omnetpp/manual/#sec:makefrag)
#
# We use it to define custom compiler flags, mainly for debugging purposes.
# Remember to run "make clean" before recompiling the project with the uncommented flags.
#

# .... =>  
#CFLAGS += -DEBUG_RNGS

# .... => throughput, 
#CFLAGS += -DEBUG_SCHEDULER

# .... => oracle registration + terminal fetch
#CFLAGS += -DEBUG_TERMINALS
